# Genesis-

A minimal blockchain genesis block implementation in C++.

## Build

```bash
mkdir build && cd build
cmake ..
cmake --build .
./genesis
```

## Output

Running `./genesis` prints the genesis block details, including its index, timestamp, data, previous hash, and SHA-256 hash.
